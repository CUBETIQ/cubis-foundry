description = "Turn a product goal into a concrete screen direction, then implement or hand off the resulting UI specification."
prompt = '''
Execute the native projection of the /design-screen workflow.

Turn a product goal into a concrete screen direction, then implement or hand off the resulting UI specification.

Execution contract:
1. Treat route selection as already resolved by this command; do not begin with skill discovery.
- native workflow surface: command.
- native specialist surface: agent-route command.
2. Load docs in this order when they exist: `docs/foundation/MEMORY.md` -> `docs/foundation/DESIGN.md` -> `docs/foundation/PRODUCT.md` -> `docs/foundation/ARCHITECTURE.md` -> `docs/foundation/STRUCTURE.md`. Add `docs/foundation/TECH.md` only when implementation constraints matter.
Attached skills:
- Load these exact skill IDs first: `frontend-design`, `design`, `web-ui-design`, `mobile-ui-design`, `desktop-ui-design`, `design-system`.
- Local skill file hints if installed: `.agents/skills/frontend-design/SKILL.md`, `.agents/skills/design/SKILL.md`, `.agents/skills/web-ui-design/SKILL.md`, `.agents/skills/mobile-ui-design/SKILL.md`, `.agents/skills/desktop-ui-design/SKILL.md`, `.agents/skills/design-system/SKILL.md`.
- Treat the skill bundle as already resolved for this workflow. Do not start with route discovery.
3. Confirm the request fits the workflow's "When to use" section before acting.
4. Execute according to "Workflow steps" and apply "Context notes".
5. Complete "Verification" checks and report concrete evidence.
6. If freshness, public comparison, or explicit research needs appear, pause implementation and load `deep-research` or route to research first.
7. For outside evidence: repo first, official docs next, Reddit/community only as labeled secondary evidence.
8. Keep runtime boundaries explicit: use Playwright MCP for web flows, prefer `mobile-mcp` for semantic mobile flows, and switch to CLI fallback when you need deterministic logs, build state, or replayable evidence.

Workflow source:
# Design Screen Workflow

## What this workflow does

Creates a screen-level brief, locks the style contract and thesis set, chooses the correct owning surface, and converts that direction into implementation-ready work.

## When to use

Use this for one screen, page, or view where the main problem is composition, hierarchy, interaction cost, or presentation.

## Agent chain

`planner -> implementer`

## Step details

1. Route through `frontend-design` first to lock the style contract, visual thesis, content or system thesis, and interaction thesis.
2. Use `design` to decide whether the work is browser-first, phone-first, or desktop-first, and whether `design-system` must refresh canonical state before screen execution.
3. Build a concrete screen brief with user goal, hierarchy, states, accessibility, motion, and anti-generic constraints.
4. Implement or hand off the design through the owning surface with platform-specific verification notes.

## Skill routing

- `frontend-design` for the public UI-design front door, style contract, and thesis capture
- `design` for routing, critique language, and execution-boundary decisions
- `web-ui-design` for browser-first screen work
- `mobile-ui-design` for app-first or phone-first screen work
- `desktop-ui-design` for workspace-first, multi-pane, or expert-oriented desktop work
- `design-system` when the screen depends on shared tokens, component vocabulary, or canonical system guidance

## Context notes

Provide screen purpose, content requirements, target surfaces, state expectations, and any brand or interaction constraints.

## Verification

The output should describe hierarchy, layout, states, styling decisions, and rejected anti-patterns clearly enough to build from.

## Output contract

```yaml
DESIGN_SCREEN_WORKFLOW_RESULT:
  direction_name: <2-5 word direction>
  style_contract:
    surface_type: <value>
    style_family: <value>
    anti_patterns:
      - <value>
  theses:
    visual: <summary>
    content_or_system: <summary>
    interaction: <summary>
  owning_surface: web-ui-design | mobile-ui-design | desktop-ui-design
  implementation_notes:
    - <note>
```

## Follow-up items

Typical next step: `/implement`

If command arguments are provided, treat them as additional user context.
'''
