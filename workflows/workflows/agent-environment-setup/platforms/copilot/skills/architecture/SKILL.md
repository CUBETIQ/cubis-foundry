---
name: architecture
description: "Compatibility alias skill. Deprecated: use architecture-designer."
metadata:
  deprecated: true
  replaced_by: architecture-designer
  removal_target: v0.6.0
---
# Architecture

## Compatibility Alias

This skill is deprecated and kept for backward compatibility.

Use `architecture-designer` as the canonical skill.

## Migration

1. Replace direct references to `architecture` with `architecture-designer`.
2. Apply the full workflow from `architecture-designer` for new work.
