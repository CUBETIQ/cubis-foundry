---
name: vercel-functions
description: "Compatibility alias skill. Deprecated: use vercel-runtime."
metadata:
  deprecated: true
  replaced_by: vercel-runtime
  removal_target: v0.6.0
---

# Vercel Functions

## Compatibility Alias

This skill is deprecated and kept for backward compatibility.

Use `vercel-runtime` as the canonical skill.

## Migration

1. Replace direct references to `vercel-functions` with `vercel-runtime`.
2. Apply the full workflow from `vercel-runtime` for new work.
