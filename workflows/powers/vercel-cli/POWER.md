````markdown
---
inclusion: manual
name: vercel-cli
description: "Compatibility alias skill. Deprecated: use vercel-delivery."
metadata:
  deprecated: true
  replaced_by: vercel-delivery
  removal_target: v0.6.0
---

# Vercel Cli

## Compatibility Alias

This skill is deprecated and kept for backward compatibility.

Use `vercel-delivery` as the canonical skill.

## Migration

1. Replace direct references to `vercel-cli` with `vercel-delivery`.
2. Apply the full workflow from `vercel-delivery` for new work.
````
