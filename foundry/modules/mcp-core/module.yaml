id: mcp-core
kind: capability
label: MCP Core
description: MCP server configuration and runtime integration layer for supported platforms.
stability: stable
profiles:
  - developer
capability:
  type: tool
  domains: [mcp, integration]
  outputs:
    - type: workflow
      path: generated/runtime-assets/mcp-core.json
      platforms: [codex, claude, copilot, gemini, antigravity]
