> This file extends [common.md](./common.md) with TypeScript-specific guidance.

## TypeScript Rules

- Preserve strict typing and avoid widening to `any` unless there is no practical alternative.
- Keep data contracts explicit at module boundaries.
- Prefer small modules and focused tests over giant utility files.
