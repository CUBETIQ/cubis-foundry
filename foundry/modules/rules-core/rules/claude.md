> This file extends [common.md](./common.md) with Claude Code specific guidance.

## Claude-Specific Rules

- Project shared rules into `CLAUDE.md` and scoped fragments into `.claude/rules.d/`.
- Prefer Claude-native hooks only when the rule cannot be expressed clearly in markdown alone.
- Keep agent and skill files in `.claude/agents/` and `.claude/skills/` aligned with their canonical Foundry sources.
