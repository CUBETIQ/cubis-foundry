> This file extends [common.md](./common.md) with GitHub Copilot specific guidance.

## Copilot-Specific Rules

- Project shared rules into `.github/copilot-instructions.md`.
- Keep custom agents in `.github/agents/` and prompt workflows in `.github/prompts/`.
- Preserve markdown frontmatter fields supported by Copilot and drop unsupported runtime-specific keys.
