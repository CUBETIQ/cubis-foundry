> This file extends [common.md](./common.md) with Python-specific guidance.

## Python Rules

- Prefer `pyproject.toml` based packaging for modern projects.
- Type annotate public APIs and keep async boundaries explicit.
- Use pytest-style verification unless the repo already standardizes on something else.
